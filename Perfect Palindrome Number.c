#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
#include <stdbool.h>
#include <ctype.h>

int main()
{
    int n, x, y, z;
    scanf("%d", &n);
    while (n--)
    {
        int num;
        scanf("%d", &num);
        if (num % 11 != 0)
        {
            printf("0\n");
        }
        else{
            
        }
        
    }
    
}